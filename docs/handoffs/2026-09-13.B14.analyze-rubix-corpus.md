# Handoff: analyze the rubix corpus (#14)

Date: 2026-09-13.
State when written: `scripts/extract_rubix_corpus.py` (merged at `ceb480c`) builds a per-host JSON
corpus of every rubix-review lens dispatch since loop-plan first wired rubix in (2026-07-17).
This host's corpus is generated and checked out at `corpus/rubix_corpus.RIT-UADV2223.json`
(gitignored - private cross-project content, never committed).
As of this run: 16 events, 34 lens dispatches, 257 parsed findings
(critical 13, high 79, medium 68, low 43, other 54 where the field parser came up short but
`raw_block`/`raw_text` still carry the source).
No distillation has been written yet - this handoff is that next step.

## Next actions

- Check `corpus/` for a second host's `rubix_corpus.<hostname>.json`; if one hasn't been dropped in
  yet, either copy it in or rerun the extractor with `--projects-dir` pointed at both hosts'
  `.claude/projects` (see `scripts/README.md`), so the distillation covers every host, not just
  this one.
- Tally findings across the combined corpus: counts and severity/type distribution, using
  `summary.severity_counts` as the starting point and reading each `finding.title`/`rationale` to
  bucket by type (correctness, scope, security, sequencing, etc.) - the parser does not do this
  categorization.
- For the 54 (this host alone) findings with unparsed severity, skim their `raw_block` by hand;
  they are not lost, just unstructured.
- Pull concrete "wrong path avoided" cases out of each event's `narrative_after` and out of
  `supplementary_docs` - `docs/2026-08-23.extraction-rubix-findings.md` in this repo is the model
  for what that evidence should look like (merged-findings table + per-finding evidence with
  verified specifics, not just a severity label).
- Write the distillation as a doc (numbers, severity/type mix, and the strongest rework-avoided
  examples with citations back into the corpus) and decide where it lives.
- Close out issue #14 once the write-up exists, or leave it open with a comment if more host data
  is still expected.

## Notes

- Extractor: `scripts/extract_rubix_corpus.py`, docs in `scripts/README.md`. `--selftest` checks
  the finding parser before trusting a fresh run.
- Corpus fields worth knowing before reading: `events[].lenses[].findings[]` (structured, best
  effort), `events[].lenses[].raw_text` (always complete), `events[].narrative_after` (the
  triage/incorporation discussion right after both lenses reported back), `memory_evidence`
  (per-project memory files mentioning rubix, e.g. `rubix-review-value.md`'s "all 10 findings
  survived triage" case), `supplementary_docs` (hand-authored rubix findings docs found in touched
  repos).
- This session (2026-09-13) is the one that built the extractor end to end; nothing about the
  corpus's content itself has been read or summarized yet.

## Transitions

- 2026-09-13: distillation written at `corpus/2026-09-13.rubix-corpus-distillation.md` (gitignored on
  purpose - the repo is public and the evidence quotes private cross-project content). Both hosts'
  corpora were combined (39 events, 73 reviewer dispatches, 440 findings after excluding 3 misparsed
  incorporation-worker blocks). Extractor defect (incorporation dispatches captured as lenses) filed
  as a GitHub issue. Issue #14 closed.
